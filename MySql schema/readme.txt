In MariaDB, creare un database "provafinale" e importare il file .sql . MariaDB va lanciato sulla stessa macchina dove viene eseguito il codice Java, sulla porta 3306.

In MariaDB, create a "provafinale" schema and import the provided .sql file into it. MariaDB must run on port 3306 on the same host where the Java code is executed.